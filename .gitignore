# GNAT / Alire build + tooling artifacts (all crates)
obj/
lib/
bin/
alire/
config/
#  Only the *generated* GNATdoc output (docs/api) is an artifact; hand-written
#  prose under docs/ (e.g. docs/design/) is tracked.
docs/api/
*.o
*.ali

# GNATprove output
gnatprove/
gnatprove-run.txt

# Provisioned embedding weights (install-model.sh); large, local.
models/
