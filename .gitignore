# GNAT / Alire build + tooling artifacts (all crates)
obj/
lib/
bin/
alire/
config/
#  Only the *generated* GNATdoc output (docs/api) is an artifact; hand-written
#  prose under docs/ (e.g. docs/design/) is tracked. gnatdoc-run.txt is the
#  teed report scripts/check-docs.sh reads to set its exit status.
docs/api/
gnatdoc-run.txt
*.o
*.ali

# GNATprove output
gnatprove/
gnatprove-run.txt

# Provisioned embedding weights (install-model.sh); large, local.
models/
